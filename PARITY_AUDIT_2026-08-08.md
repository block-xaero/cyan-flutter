# Parity audit — cyan_flutter (Windows) vs cyan-iOS (Mac), 2026-08-08

Raw findings from the five-axis adversarial audit (workflow run `wf_5b221b00-aee`).
Captured mid-run at Rick's pause; the signup/role/claims axis and the skeptic
verdicts had not landed yet. Journal:
`C:\Users\ricky\.claude\projects\C--Users-ricky\40086c83-1d98-4c7c-8c78-1de08c996708\subagents\workflows\wf_5b221b00-aee\journal.jsonl`

## AXIS — End-to-end DCC spine drivability (ingest â†’ review â†’ edit â†’ conform â†’ colour â†’ audio â†’ QC â†’ delivered master), in both HUMAN-GATED and AUTOPILOT modes

**State:** The Windows Flutter app cannot drive any part of the DCC spine today, and the reason is not missing code â€” it is missing wiring. `lib/main.dart` boots `WorkspaceScreen`, which mounts the LEGACY pre-parity widgets (`icon_rail.dart`, `board_detail_view.dart` with its canvas/notebook/notes Jupyter faces). Nothing outside `lib/widgets/parity/` imports anything from `lib/widgets/parity/` â€” verified by `grep -rn "widgets/parity" lib/ | grep -v "^lib/widgets/parity/"` returning zero hits â€” so `ParityWorkflowView`, `ParityDashboardView`, `ParityBoardContainer`, `ParityHomeShell`, `ParitySpineView`, `ParityReviewPlayerView`, `ParityVideoFace`, `ParitySourcesSheet` and the whole Ops/audit tree are reachable only from `test/`. An operator on Windows therefore has no compile, no run, no gate, no autopilot switch, no ingest, no review player and no delivered master. Underneath that blocker there are also real per-control gaps that will still bite the moment the shell is wired: a Deploy button literally `onTap: null`, a decorative step overflow menu, and a park state (`awaitingInput`) with no action anywhere on the Dashboard.

### [blocker] The entire parity face tree is dead code at runtime â€” the shipping app still mounts the pre-parity Jupyter board
*id: `parity-tree-orphaned-from-running-app`*

- **Mac does:** App/CyanApp.swift:92 mounts `ContentView()`; Views/ContentView.swift:22 mounts `WorkspaceViewNew()`; Views/WorkspaceViewNew.swift:223 and :252 mount `BoardContainerViewNew(boardId:groupId:)`; Views/BoardContainerViewNew.swift:407-452 `faceContent` switches the live board onto `WorkflowView` (plain-English steps + Review/Run/Autopilot/Schedule/Sources/Deploy), `NotesEditorView`, `DashboardFace` (live DAG + approve/reject gates) and `ReviewPlayerView`/`VideoPlayerFace`. That chain is how every spine station is reached on the Mac.
- **Flutter does:** lib/main.dart:145 `_AppRoot.build` returns `const WorkspaceScreen()`. lib/screens/workspace_screen.dart:7-12 imports ONLY legacy widgets (`widgets/icon_rail.dart`, `widgets/file_tree_widget.dart`, `widgets/all_boards_grid.dart`, `widgets/full_chat_view.dart`, `widgets/board_detail_view.dart`); :114-118 opens a selected board into `BoardDetailView`, whose faces are `enum BoardFace { canvas, notebook, notes }` (lib/widgets/board_detail_view.dart:22-31) with markdown/python/sql/mermaid/model cells â€” exactly the surface WorkflowView.swift:5-10 says it REPLACED. `grep -rn "widgets/parity" lib/ | grep -v "^lib/widgets/parity/"` returns nothing: `ParityHomeShell` (parity_home_shell.dart:31), `ParityBoardContainer` (parity_board_container.dart:43), `ParityWorkflowView`, `ParityDashboardView`, `ParityOpsScaffold`/`ParityOpsRuns`/`ParityRunAudit` have zero construction sites outside `lib/widgets/parity/` and `test/`. lib/ contains exactly one entrypoint (`ls lib/*.dart` â†’ `main.dart`).
- **Gap:** On Windows the operator opens a board and gets a canvas/notebook/notes cell editor. There is no Review (compile), no Run, no autopilot mode menu (the only `_AutopilotControl` in the codebase lives in the unmounted parity_workflow_view.dart:774), no Dashboard face, no approval gate, no incidents, no Ops console and no run audit. Neither gate mode is drivable because neither the run surface nor the mode switch is on screen.
- **Fix sketch:** Point `_AppRoot` (lib/main.dart:145) at `ParityHomeShell`, and route board selection to `ParityBoardContainer(boardId:)` instead of `BoardDetailView`. Delete or quarantine `workspace_screen.dart`/`board_detail_view.dart`/`icon_rail.dart` so there is one shell, not two.

### [blocker] Ingest â€” the first station of the spine â€” has no door anywhere in lib/
*id: `ingest-sources-has-no-door`*

- **Mac does:** Views/WorkflowView.swift:378-389: the Workflow toolbar renders a `Sources` button (`accessibilityIdentifier("workflow.sources")`) that presents `SourcesSheet(viewModel: sourcesVM, isPresented: $showSources)` â€” the watched-folder / S3 / Frame.io C2C sensors whose new media materializes per-asset runs. WorkflowView.swift:79-81 also renders `IngestRunsStrip(viewModel: sourcesVM)` so the materialized per-asset runs are visible on the face.
- **Flutter does:** `grep -rn "ParitySourcesSheet" --include=*.dart .` finds it ONLY in lib/widgets/parity/parity_sources_sheet.dart (its own declaration) and in test/sources_ingest_test.dart (9 mounts). No file in lib/ ever constructs it. The Dart workflow toolbar (lib/widgets/parity/parity_workflow_view.dart:707-761) carries Templates / Review / Run / Autopilot / Deploy / Reset and nothing else â€” no Sources button, no ingest runs strip. `lib/providers/ingest_sources_controller.dart` has no consumer in lib/ either.
- **Gap:** An operator cannot point the app at a watched folder or scan it, so no master ever becomes addressable and nothing downstream (conform, colour, delivery) has an asset to work on. The spine cannot be started at all, in either gate mode.
- **Fix sketch:** Add a `Sources` `_ToolButton` to `_Toolbar` in parity_workflow_view.dart that `showDialog`s `ParitySourcesSheet(boardId:, tenantId:)` (the same pattern `_openTemplates` already uses at :237-259), and mount the runs strip under the status strip.

### [blocker] There is no `video` board face, so the review player, its scrubber and the AE graphics rail can never be opened
*id: `no-video-face-review-player-unreachable`*

- **Mac does:** Views/BoardFace.swift:16 declares `case video = "video"`; BoardContainerViewNew.swift:58-63 `availableFaces` appends `.video` whenever `videoAsset != nil`; :439-442 `faceContent` mounts `ReviewPlayerView(boardId:asset:)` when the asset is under review and `VideoPlayerFace(boardId:asset:)` otherwise; :287-289 binds âŒ˜4 to it; DashboardView.swift:1055-1093 puts an "Open in review" chip on every asset frame that posts `.openReviewPlayer`, which BoardContainerViewNew.swift:266-268 consumes to switch the board onto `.video`.
- **Flutter does:** lib/models/board_face.dart:32 â€” `enum BoardFace { workflow, notes, dashboard }`; the file's own header comment at :21-22 concedes "Swift also carries a `.video` face â€¦ that surface is the review-player stage, not this container." lib/widgets/parity/parity_board_container.dart:137-141 `_face()` is a three-arm switch with no video arm. `ParityReviewPlayerView` is constructed only in test/review_player_test.dart:62 and :580; `ParityVideoFace` only in test/video_face_test.dart:55. lib/widgets/parity/parity_dashboard_view.dart has no asset frame and no "Open in review" affordance at all (no `openReviewPlayer` equivalent anywhere in lib/).
- **Gap:** Even after the shell is wired, the REVIEW station of the spine is unreachable: no player, no scrubber lane, no timecoded notes, no graphics rail, and no way to jump from a dashboard asset card into review. `review_player_controller.dart` (approve/reject/approveEdited/produceMaster/undoCut/redoCut, 885 lines) has no UI door.
- **Fix sketch:** Add `video` to `BoardFace` + `rawValue`/`label`/`icon`, gate the tab on a resolved `boardVideoMedia(boardId)` read (the seam verb already exists at lib/ffi/cyan_backend.dart:686), add the fourth arm to `_face()` mounting `ParityReviewPlayerView`/`ParityVideoFace`, and add an "Open in review" tap on the dashboard's asset that switches the container to it.

### [blocker] The delivered-master end state has no reachable trigger â€” both `produce_master` call sites are in unmounted surfaces
*id: `delivered-master-has-no-reachable-trigger`*

- **Mac does:** The Mac reaches delivery through the composed faces: DashboardView.swift:820-893 gives every settled/parked step its Approve / Complete / Re-run-from-here controls, and Views/ReviewPlayerView.swift plus ViewModels/ReviewPlayerViewModel.swift own the confirm-gate â†’ conform â†’ produce-master hand-off that ends in a delivered master path.
- **Flutter does:** `grep -rn "produce_master" lib/` hits exactly two callers: lib/providers/review_player_controller.dart:816 (`ParityReviewPlayerView`, unmounted â€” see finding 3) and lib/providers/spine_controller.dart:825 `produceMaster()`, whose only UI is `ParitySpineView`, constructed only in test/spine_e2e_test.dart:71. `SpineController` (lib/providers/spine_controller.dart) is a complete end-to-end driver â€” `cloneSpine()` :520, `ingestSources()` :570, `compile()` :601, `advance()` :621, `releaseReviewGate()` :724, `confirmPictureLock()` :790, `produceMaster()` :819 setting `deliveredMaster` :836 â€” and `ParitySpineView` wires every one of them (parity_spine_view.dart:256, 301, 528, 543, 552, 561, 728, 736). None of it is reachable from `main.dart`.
- **Gap:** There is no click path in the shipping Windows app that produces a delivered master, and no surface that displays one. The spine's terminal state is unobservable.
- **Fix sketch:** Either give `ParitySpineView` a door (a rail entry or a per-board tab, passing boardId/tenantId), or â€” better matched to the Mac â€” reach delivery through the composed faces once findings 1 and 3 land, so `produceMaster` is driven from the review player's hand-off gate.

### [major] The Workflow toolbar's Deploy / Unlock button is wired to `onTap: null` while rendering as enabled
*id: `deploy-button-ontap-null`*

- **Mac does:** Views/WorkflowView.swift:391-408 â€” when unlocked, `Deploy` calls `lockStore.lock(viewModel.boardId)` and posts `.switchToDashboardFace` so the operator lands on the running workflow; when locked, `Unlock` calls `lockStore.unlock(viewModel.boardId)` to re-enable authoring. Each toggle emits an obs event.
- **Flutter does:** lib/widgets/parity/parity_workflow_view.dart:745-751 â€” `_ToolButton(icon: locked ? Icons.lock_open : Icons.lock, label: locked ? 'Unlock' : 'Deploy', tint: MonokaiTheme.purple, enabled: hasSteps, onTap: null)`. `_ToolButton.build` (:855-880) computes `GestureDetector(onTap: enabled ? onTap : null)` and `Opacity(opacity: enabled ? 1 : 0.5)`, so with `enabled: hasSteps == true` the control paints at full opacity and full tint and swallows every tap. There is no other deploy/lock call site: `grep -rn "isDeployed\|lock" lib/providers/` shows `isDeployed` is only ever READ (parity_workflow_view.dart:392, :709), never written.
- **Gap:** The operator sees a live-looking Deploy button, clicks it, and nothing happens â€” no lock, no read-only banner, no jump to the run surface. The deploy-and-lock lifecycle is absent from the port entirely.
- **Fix sketch:** Thread an `onDeploy`/`onUnlock` callback into `_Toolbar` that writes the lock through the seam and then asks the container to switch to `BoardFace.dashboard`; until the write exists, pass `enabled: false` so the control does not lie.

### [major] The Workflow step row's â‹¯ is a plain Icon â€” Edit / Approve / Retry / Undo / Redo / Delete and tap-to-edit are all missing
*id: `workflow-step-menu-is-a-decoration`*

- **Mac does:** Views/WorkflowView.swift:994-1012 â€” every `StepRow` carries a `Menu { Edit Â· Approve Â· Retry Â· Divider Â· Undo edit Â· Redo edit Â· Divider Â· Delete }` with `accessibilityIdentifier("workflow.step.menu.<uid>")`, wired to `viewModel.updateStep` / `approve(stepId:)` / `retry(stepId:)` / `undoStepEdit` / `redoStepEdit` / `deleteStep` (:512-521, :1000-1001). The step text itself has `.onTapGesture { onEdit() }` (:944), and rows drag-reorder with persistence (:538-548).
- **Flutter does:** lib/widgets/parity/parity_workflow_view.dart:962-963 â€” `const Icon(Icons.more_horiz, size: 16, color: MonokaiTheme.textMuted)`. It is a `const Icon`, not wrapped in any `GestureDetector`, `InkWell` or `PopupMenuButton`. The step text at :956-961 is a bare `Text` with no gesture. `_StepRow`'s entire callback surface is `onPickPlugin` + `onAccept` (:911-912). No Draggable/DragTarget. The seam already carries `updateWorkflowStep` (lib/ffi/cyan_backend.dart:119) and `deleteWorkflowStep` (:127) â€” both unused by any widget.
- **Gap:** An authored step is immutable and undeletable from the Flutter Workflow face: a typo, a wrong @plugin binding, or an unwanted step can only be worked around by rebuilding the board. Per-step Approve/Retry are also absent here.
- **Fix sketch:** Replace the decorative icon with a `PopupMenuButton` whose items call `backend.updateWorkflowStep` / `deleteWorkflowStep` / `pipelineApprove` / `pipelineRetry` / `stepEditTravel`, and give the step `Text` an `onTap` that flips the row into an inline edit `TextField` (the composer's `_openPicker` machinery already supports a per-step picker keyed by `step.id`, parity_workflow_view.dart:322-328).

### [major] A step parked on `awaitingInput` gets no control at all on the Flutter Dashboard â€” the run wedges
*id: `awaiting-input-park-has-no-control`*

- **Mac does:** Views/DashboardView.swift:595-628 `parkedSteps` renders every `step.state == .awaitingInput` as an amber "Awaiting input" card with its finding text and a `Re-run` button bound to `viewModel.rerunParked(stepId:)` (`accessibilityIdentifier("dashboard.rerun.<id>")`, keyboard shortcut `R`). ViewModels/DashboardViewModel.swift:1013-1028 `rerunParked` resolves upstream to the comment/sense step when there is one so new reviewer notes are re-read before the parked step re-dispatches. The DAG box also badges it (:713-719).
- **Flutter does:** lib/providers/dashboard_controller.dart:130-132 â€” `bool get isGateOpen => state == DashboardStepState.awaitingApproval || state == DashboardStepState.needsLens;` â€” `awaitingInput` is excluded, so :280 `get gates` never lists it and no `_GateCard` is drawn. In lib/widgets/parity/parity_dashboard_view.dart:230-235, `onRetry` is supplied only when `s.state == DashboardStepState.failed` and `onRunNow` only when `s.state == DashboardStepState.pending`. `DashboardStepState.awaitingInput` therefore reaches `_StepRow` with both callbacks null; :757 and :766 then omit both buttons. There is no `rerunParked` on `DashboardController` (its whole action set is approve/reject/runNow/retry at :522-586).
- **Gap:** When the engine parks a mechanical step for a missing input â€” the canonical case being conform with no confirmed edits, i.e. exactly the middle of the spine â€” the Flutter Dashboard shows a yellow dot, the label 'Awaiting input', and zero affordances. The operator does the work in the NLE and then has no button to resume the run. This holds in both gate modes.
- **Fix sketch:** Include `awaitingInput` in `isGateOpen` (or add a parallel `parked` list), add `DashboardController.rerunParked(stepId)` mirroring the Swift upstream-resolution then calling `pipelineRetry`, and render a `Re-run` `_GateButton` for those rows.

### [major] Dashboard run controls absent: pause/resume, whole-run reset, per-step reset, replay-from-here, assign reviewer, findings, and pre-run compile/run/schedule
*id: `dashboard-missing-run-controls`*

- **Mac does:** Views/DashboardView.swift:427-456 â€” `Pause` (`dashboard.run.pause`) / `Resume` (`dashboard.run.resume`) / `Reset run` (`dashboard.run.reset`) in the run header. :863-892 â€” per-step `Retry` + `Reset` on failure, and `Re-run from here` (`dashboard.step.rerun_from`) + `Reset` (`dashboard.step.reset`) on any done/approved step, i.e. the replay path. :526-544 â€” the inline `assign reviewer` field + `Set` for an unassigned review hold (`dashboard.assignee.field` / `.set`). :769-819 â€” the Findings panel (timecoded QC + model summary) so an approval is informed. :315-326 â€” the pre-run `PipelinePreviewView` is wired with `onCompile`, `onRun`, `onSchedule`, `onReset` (PipelinePreviewView.swift:81-135 draws those buttons), and :355-414 shows the schedule receipt/next-run banner. ViewModels/DashboardViewModel.swift:131-161 owns pauseRun/resumeRun/resetRun; :1030-1044 `reset(stepId:)`; :685 `assignReviewer`; :1117 `scheduleWorkflow`.
- **Flutter does:** lib/widgets/parity/parity_dashboard_view.dart:253-291 `_RunHeader` is a label + status pill only â€” no Pause/Resume/Reset. `_live()` (:185-246) emits only gate Approve/Reject, `Retry` on failed and `Run now` on pending. `_preRun()` (:139-181) emits the DAG and a step list and NO buttons â€” there is no Compile, Run, Schedule or Reset on the Dashboard face at all. lib/providers/dashboard_controller.dart exposes exactly `approve` (:522), `reject` (:535), `runNow` (:556), `retry` (:579) â€” no pause/resume/resetRun/reset(stepId)/assignReviewer/schedule, even though the seam already carries `pipelineResetStep` (lib/ffi/cyan_backend.dart:420) and `boardSetReviewAssignee` (:680). No step-findings rendering exists (`DagStep` in dashboard_controller.dart:83-101 has no findings field). No schedule verb exists on either seam: `grep -n "schedule" lib/ffi/cyan_backend.dart lib/lens/lens_api.dart` returns nothing.
- **Gap:** A run cannot be paused, cannot be reset, cannot be replayed from an earlier step, and an unassigned review hold cannot be assigned â€” so a producer-scoped gate that came back unassigned is permanently unclearable. Approvals are made blind (no findings panel). Recurring deployment (Schedule) is absent from the port entirely, so the autopilot story stops at one manual Run.
- **Fix sketch:** Add pause/resume flags + `resetRun()` + `resetStep(stepId)` + `assignReviewer(user)` to `DashboardController` (all four have seam verbs already), render them in `_RunHeader`/`_StepRow`, carry `findings` through `DagStep` from `pipelineStatus`, and add a `schedule` method to `LensApi` plus a Schedule control on the pre-run surface.

## AXIS — Shell + reachability: can a running Windows operator actually get to every parity face, and is the app shell itself at parity with the Mac?

**State:** The entire parity port is dead code at runtime. `lib/widgets/parity/` â€” 38 files, 44 public widgets, plus ~16 dedicated providers â€” is imported by ZERO app-code files; `grep -rn "parity/" lib/ | grep -v "^lib/widgets/parity/"` returns nothing, and every parity class name resolves only inside the parity folder and `test/*.dart`. What actually runs on Windows is `main.dart -> _AppRoot -> WorkspaceScreen`, an older 4-door shell built from `lib/widgets/icon_rail.dart`, `status_bar.dart`, `board_detail_view.dart` â€” a shell with no Marketplace door, a "Lens AI" door that renders "Coming soon", a Settings button that shows a SnackBar, three rail buttons wired to `onTap: () {}`, a hardcoded green "Synced" badge, and a board cube offering Canvas/Notebook/Notes instead of the Mac's Workflow/Notes/Dashboard(/Video). Mounting `ParityHomeShell` alone would not fix it: that shell default-constructs every face, so `onOpenBoard` is null and `ParityBoardContainer` (the board cube) is referenced by literally nothing in the repo. Mac views with no Dart file at all: `AEQueueView`, `ForgeView`, `CloudOperationsView`, `NotesStructuringView`, `DiagramOverlayView`/`DiagramResultPanel`, `MentionPicker`, `MonacoEditorView`, `CanvasAIChatBar`, `PluginBadge`, `SkillInfo` (all zero case-insensitive hits across `lib/`). The parity faces themselves are NOT stubs â€” the only `_Placeholder`/"coming soon" strings in `lib/widgets/parity/` are legitimate empty-state cards in `parity_dashboard_view.dart` and `parity_live_run_view.dart`; every "coming soon" the operator can actually reach lives in the legacy shell.

### [blocker] Every parity face is unreachable: nothing in app code imports lib/widgets/parity/
*id: `parity-island-never-mounted`*

- **Mac does:** cyan-iOS/Cyan/Cyan/Views/ContentView.swift:16-25 â€” authenticated users land in `WorkspaceViewNew()`, which is the shell that mounts IconRail + FileTreeView + BoardGridView + BoardContainerViewNew + StatusBar + the Settings/Profile/NewBoard/NewChat/CloudOps sheets (WorkspaceViewNew.swift:71-147). Every Mac surface hangs off that one tree.
- **Flutter does:** C:\cyan\cyan_flutter\lib\main.dart:114 `home: const _AppRoot()`, :132-142 `_AppRoot.build` -> SplashScreen | LoginScreen | `const WorkspaceScreen()`; routes at :115-119 are only /login, /workspace, /profile. `WorkspaceScreen` (lib/screens/workspace_screen.dart:30,53) mounts the LEGACY `IconRail` and `StatusBar` from lib/widgets/, never a parity widget. PROOF OF ISOLATION: `grep -rn "parity/" --include=*.dart lib/ | grep -v "^lib/widgets/parity/"` returns ZERO lines. Grepping each of the 44 public parity class names (ParityHomeShell, ParityIconRail, ParityStatusBar, ParityBoardContainer, ParitySettingsView, ParityWorkspaceView, ParityReviewPlayerView, ParityNotebookView, ParitySpineView, ParityOnboardingGate, ParityRosterPanel, ParityConstitutionEditor, ParitySourcesSheet, ParityPluginsWorkspace, ParityVideoFace, ParityLiveRunView, ParityBoardFilesView, ParityOpsMetering, â€¦) across lib/ yields hits ONLY inside lib/widgets/parity/. `ParityHomeShell` itself appears in exactly two places: its own definition (parity_home_shell.dart:31) and a comment in parity_icon_rail.dart:7 â€” plus six `pumpParity(tester, const ParityHomeShell())` calls in test/shell_test.dart:44,53,80,91,101,113. The state layer is the same island: shell_provider, board_face_provider, workspace_provider, spine_provider, review_player_provider, notebook_provider, live_run_provider, dashboard_provider, marketplace_provider, templates_provider, onboarding_session_provider, settings_identity_provider, notes_ledger_provider, ingest_sources_controller, workflow_authoring_provider and mesh_status_provider are each referenced only from lib/widgets/parity/*.dart.
- **Gap:** A Windows operator running the built app can never see ANY parity face. PARITY_TRACKER.md marks rows 1-12 'ci-green', but green means a widget test pumped the widget in isolation â€” the shipped binary shows the pre-parity shell. Every other axis's findings are moot until this is closed.
- **Fix sketch:** Change `lib/main.dart:142` to return the parity shell instead of `WorkspaceScreen` (and delete/retire lib/screens/workspace_screen.dart + home_screen.dart, which is itself dead â€” `HomeScreen` has zero references in lib/). That alone is not sufficient; see finding `parity-shell-mounts-faces-with-null-callbacks`.

### [blocker] The rail the operator actually sees has no Market door, a 'Coming soon' Lens door, a Console toggle nothing reads, and three buttons wired to `onTap: () {}`
*id: `live-rail-not-at-parity`*

- **Mac does:** Views/Components/IconRail.swift:13-53 â€” `NavigationMode` has FIVE doors: files(âŒ˜1), boards(âŒ˜2), chat(âŒ˜3), market(âŒ˜4), lens(âŒ˜5). :92-152 â€” the bottom section is New Board (âŒ˜N, badge, -> `onNewBoard`), New Chat (âŒ˜â‡§C -> `onNewChat`), Cloud Operations (-> `onCloudOpsTap`), Settings (âŒ˜, -> `onSettingsTap`), Profile (âŒ˜â‡§P -> `onProfileTap`). WorkspaceViewNew.swift:77-84 binds all five callbacks; :131-146 opens `CreateBoardSheet` (IconRail.swift:263), `NewChatSheet` (IconRail.swift:400), `SettingsView`, `CloudOperationsView`. WorkspaceViewNew.swift:316-323 `marketView` -> `MarketplaceView(targetGroupId:)`; :331-342 `lensView` -> `LensAIView(boardId:)`. IconRail.swift:6 explicitly records that the integrations console and split toggle were REMOVED from the Mac rail.
- **Flutter does:** C:\cyan\cyan_flutter\lib\widgets\icon_rail.dart is the rail `WorkspaceScreen` mounts. It has FOUR nav buttons (:27-54): Explorer, Boards, Chat, and 'Lens AI' (âŒ˜4) whose onTap is `showEvents()` -> `ViewMode.events` (lib/providers/navigation_provider.dart:7-12 has only allBoards/explorer/chat/events) -> workspace_screen.dart:79-81 renders `_EventsView`, which is an icon + 'Events & Activity' + literal text **'Coming soon'** (workspace_screen.dart:234-269). There is NO Market door anywhere in lib/ outside the parity island. :67-74 a 'Console' button toggles `showConsoleProvider`; grepping that provider across lib/ gives only navigation_provider.dart:38 (declaration) and icon_rail.dart:17,73,281 â€” no surface ever reads it, so the door is a light switch attached to nothing. :84 Split `onTap: () {},  // TODO: split view toggle` (and Split does not exist on the Mac rail at all). :104 New Board `onTap: () {},  // TODO`. :114 New Chat `onTap: () {},  // TODO`. There is no Ops/Cloud console button.
- **Gap:** Marketplace and the Ops console are not reachable by any route on Windows. Lens is a dead placeholder screen. 'New Board' and 'New Chat' â€” the two primary creation affordances on the Mac â€” are visible, hoverable, clickable buttons that do nothing. 'Console' and 'Split' are phantom controls, one of which the Mac deliberately deleted.
- **Fix sketch:** Replace `lib/widgets/icon_rail.dart` with `ParityIconRail`, but first extend `ShellDoor` (lib/providers/shell_provider.dart:23-37) and `ParityIconRail` (parity_icon_rail.dart:36-43, which renders ONLY the five nav doors) with the Mac's bottom action group: onNewBoard -> the `_NewBoardDialog` already in parity_explorer_tree.dart:770, onNewChat -> a board picker, onCloudOps -> `OpsScaffold`, onSettings -> `ParitySettingsView`, onProfile -> the existing /profile route. Delete the Split and Console buttons.

### [blocker] Settings is a SnackBar; ParitySettingsView (838 lines, 3 tabs) is orphaned
*id: `settings-unreachable`*

- **Mac does:** Views/SettingsView.swift:134-149 â€” `SettingsTab` = appearance / shortcuts / groups / storage / about, rendered as a sidebar+detail sheet (:115-123). It carries the theme toggle, font size + editor font, the keyboard-shortcut table rendered from the `WorkspaceShortcut` registry, group export/import, Clear Cache / Compact Database / Export Data / Import Data (:437-492), and Delete Identity (:509-537). Opened from IconRail.swift:133-141 (âŒ˜,) via WorkspaceViewNew.swift:79 -> :139-143 `.sheet { SettingsView() }`.
- **Flutter does:** C:\cyan\cyan_flutter\lib\widgets\icon_rail.dart:128-134 the Settings rail button calls `_showSettings(context)`, defined at :151-155 as `ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Settings coming soon')))`. That is the ONLY settings affordance the running app has: `grep -rn "SettingsView|SettingsScreen|class .*Settings" lib/` outside the parity folder returns only a doc-comment in lib/providers/settings_identity_provider.dart. `ParitySettingsView` (lib/widgets/parity/parity_settings_view.dart:45, with `_PreferencesTab`:221, `_GroupsTab`:456, `_IdentityTab`:638) has ZERO references anywhere in lib/ â€” not even from another parity file.
- **Gap:** There is no way for a Windows operator to change a preference, see their shortcut list, export/import a group bundle, or manage/delete their identity. The Mac offers all of it behind âŒ˜, and a rail button.
- **Fix sketch:** Wire the rail's Settings button to `showDialog`/`Navigator.push` of `ParitySettingsView` and drop `_showSettings`. The face and its provider (settings_identity_provider.dart) already exist and are test-green.

### [blocker] The live board cube offers Canvas/Notebook/Notes â€” the Mac's Dashboard and Video faces are unreachable, and Canvas is a face the Mac deleted
*id: `board-cube-wrong-faces`*

- **Mac does:** Views/BoardFace.swift:12-27 â€” `BoardFace` = notebook(label 'Workflow') / notes / dashboard / video; `standardFaces` = [notebook, notes, dashboard]; :66 comments that 'Canvas/freeform faces were removed'. :85-90 `resolved(saved:deployment:)` makes a DEPLOYED board with a dashboard open on Dashboard. BoardContainerViewNew.swift:58-64 appends `.video` when the board has a media asset; :326-332 the `BoardFaceSelector`; :409-442 `faceContent` mounts WorkflowView / NotesEditorView / DashboardFace / ReviewPlayerView-or-VideoPlayerFace. :337-399 the header also carries the chat-lane toggle with a live unread badge (`board.chat.toggle`), a paperclip that opens `BoardFilesView` as a sheet (:272-274), Share, and a More menu (Rename / Duplicate / Export PDF / Export Markdown / Delete).
- **Flutter does:** The reachable board surface is `BoardDetailView` (lib/widgets/board_detail_view.dart:33), mounted from workspace_screen.dart:115. Its `enum BoardFace` at :22-31 is exactly `canvas('canvas',â€¦), notebook('notebook',â€¦), notes('notes',â€¦)` â€” no dashboard, no video. `_buildFaceSelector` (:160-175) renders `BoardFace.values`, so Canvas is a visible tab; `_buildContent` (:177-186) maps it to `_CanvasView` (:310) which renders the literal text 'Whiteboard coming soon' at :334. `_handleMenuAction` (:205-217) has `case 'delete': // TODO`, `case 'rename': // TODO`, `case 'duplicate': // TODO` â€” three menu items that no-op. There is no Files/paperclip affordance and no unread badge. Meanwhile `ParityDashboardView`, `ParityVideoFace`, `ParityReviewPlayerView`, `ParityBoardFilesView`, `ParityNotesLedger` and the deployed-board resolver `boardOpeningFaceProvider` (lib/providers/board_face_provider.dart:25-35, which implements `BoardFace.resolved`) exist but are all unreachable â€” `ParityBoardContainer`, the widget that would mount them, has ZERO references across the whole repo including other parity files and tests' app code.
- **Gap:** An operator on Windows cannot open a board's Dashboard (the running workflow: DAG, gates, approve/reject) or its Video/review face at all, cannot see or attach board files, and is instead shown a Canvas tab that dead-ends on 'Whiteboard coming soon'. A deployed board opens on the editable Workflow instead of its live Dashboard. Rename/Duplicate/Delete are dead menu items.
- **Fix sketch:** Route board opens to `ParityBoardContainer(boardId:)` (parity_board_container.dart:43) instead of `BoardDetailView`, and delete lib/widgets/board_detail_view.dart. Add the Mac header's paperclip -> `ParityBoardFilesView` sheet, the chat-lane toggle, and the More menu to `_BoardHeader` (parity_board_container.dart:148-196), plus the `.video` face when `ParityVideoFace` has an asset.

### [blocker] Even the parity shell cannot open a board: ParityHomeShell default-constructs every face, so onOpenBoard is null
*id: `parity-shell-mounts-faces-with-null-callbacks`*

- **Mac does:** WorkspaceViewNew.swift:221-241 and :249-268 â€” `BoardGridView(onOpenBoard: { item in selectedBoard = SelectedBoardInfo(from: item) })`, and when `selectedBoard != nil` the shell mounts `BoardContainerViewNew(boardId:groupId:).id(board.id)`. WorkspaceViewNew.swift:322 threads a real group into `MarketplaceView(targetGroupId:)`. The Mac shell is the thing that owns board selection and hands it to the cube.
- **Flutter does:** C:\cyan\cyan_flutter\lib\widgets\parity\parity_home_shell.dart:61-67 `_surfaceFor` returns `const ParityExplorerTree()`, `const ParityBoardsGrid()`, `const ParityChatView()`, `const ParityLensView()`, `const ParityMarketplace()` â€” every one default-constructed. `ParityExplorerTree.onOpenBoard` (parity_explorer_tree.dart:104) is therefore null, so its open handler at :216 `if (row.board != null) widget.onOpenBoard?.call(row.board!)` is a no-op. `ParityBoardsGrid.onOpenBoard` (parity_boards_grid.dart:20-22) is null, so the card tap at :156 `onTap: () => onOpenBoard?.call(b)` is a no-op. `ParityMarketplace` (parity_marketplace.dart:41-65) gets groupId=null (installs refuse), sessionRole=null (so `forgeEntryGate(null)` locks the 'Build a custom tool' entry permanently, parity_marketplace.dart:216 `onTap: gate.locked ? null : onTap`), onUse=null and onBuildCustomTool=null. `ParityHomeShell` never holds a selected-board id and never mounts `ParityBoardContainer`.
- **Gap:** Simply pointing main.dart at ParityHomeShell would ship a shell where clicking a board in the tree or the grid does nothing, and where the marketplace can neither install nor forge. The board faces (Workflow/Notes/Dashboard) would remain unreachable, and so would every face hanging off them (`ParityTemplatePicker`, `ParitySourcesSheet`, `ParityNotesLedger`, `ParityConstitutionEditor`, `ParityRosterPanel`, `ParitySpineView`, `ParityRunAudit`, `ParityLiveRunView`, `ParityNotebookView`, `ParityPluginsWorkspace`, `ParityOnboardingGate`, `ParityWorkspaceView`).
- **Fix sketch:** Give ParityHomeShell a `selectedBoardId`/`selectedGroupId` StateProvider (mirroring WorkspaceViewNew's `@State selectedBoard`), pass `onOpenBoard:` into `ParityExplorerTree` and `ParityBoardsGrid`, and have `_surfaceFor` render `ParityBoardContainer(boardId:, onBack: clearSelection)` when a board is selected. Thread `groupId` and `sessionRole` (from shellIdentityProvider / onboarding_session_provider) into `ParityMarketplace`.

### [major] Only 5 of the Mac's 11 global shortcuts exist, and all of them are bound to Meta (the Windows key) rather than Control
*id: `shortcuts-meta-on-windows-and-registry-missing`*

- **Mac does:** Views/WorkspaceShortcuts.swift:44-84 â€” the single registry: explorer âŒ˜1, boards âŒ˜2, chat âŒ˜3, market âŒ˜4, lens âŒ˜5, newBoard âŒ˜N, newChat âŒ˜â‡§C, toggleSidebar âŒ˜B, settings âŒ˜,, profile âŒ˜â‡§P, closeBoard Esc. :136-157 is the pure reducer the View runs (WorkspaceViewNew.swift:403-441) and the Settings panel renders from. The file's header comment (:11-16) exists specifically because dead/duplicate shortcut bindings were treated as a bug class.
- **Flutter does:** C:\cyan\cyan_flutter\lib\widgets\icon_rail.dart:256-292 `IconRailShortcuts` is the only global shortcut host in the running app (workspace_screen.dart:21 wraps the Scaffold in it). It binds exactly five chords, all with `LogicalKeyboardKey.meta`: meta+1/2/3/4 and meta+backquote. On Windows `LogicalKeyboardKey.meta` is the Windows/Super key, not Control. Missing entirely: lens(5), newBoard(N), newChat(â‡§C), toggleSidebar(B), settings(,), profile(â‡§P), closeBoard(Esc). The rail's own tooltips and the empty-state hints (workspace_screen.dart:178,190 `_KeyHint('âŒ˜1')`, `_KeyHint('âŒ˜2')`) print Mac glyphs to a Windows operator. Note the parity island is inconsistent about this too: parity_workspace_view.dart:125 correctly uses `SingleActivator(..., control: true)` while parity_board_container.dart:113 uses `SingleActivator(_faceDigits[i], meta: true)`.
- **Gap:** On Windows the five shortcuts that exist are triggered by Win+1..4 / Win+` â€” chords the OS largely intercepts â€” so in practice the app has no working keyboard navigation at all, while the UI advertises âŒ˜1/âŒ˜2. Six more Mac shortcuts have no binding of any kind, and there is no Settings panel to discover them from (see settings-unreachable).
- **Fix sketch:** Port `WorkspaceShortcut` as a Dart enum + pure reducer (one file, mirroring WorkspaceShortcuts.swift), bind it once at the shell with `control: true` on Windows/Linux and `meta: true` on macOS, render the same list in `ParitySettingsView`'s shortcuts section, and fix parity_board_container.dart:113 to use the same platform switch. Replace the `âŒ˜` glyphs in `_KeyHint` with the platform's modifier.

### [major] The status bar's sync state is a hardcoded green 'Synced'; connection tier, transfers, tenant and lens connectivity are absent
*id: `status-bar-hardcoded-synced`*

- **Mac does:** Views/StatusBar.swift â€” :121-129 a lens badge driven by real state (`lens.state == .connected ? "cloud.fill" : "cloud.slash"`, id `statusbar.lens`); :154-162 the tenant badge (`statusbar.tenant`); :169-173 the selection breadcrumb; :194-247 an active-transfers button opening a popover listing each transfer's filename and percent; :253-273 object + peer counts; :284-286 `viewModel.connectionTier` icon+label; :297-314 `viewModel.syncState` icon + `displayText`; :328-364 the profile button (initials disc + name) that opens ProfileView. All fed by ViewModels/StatusBarViewModel.swift + SyncStatusMachine.swift.
- **Flutter does:** C:\cyan\cyan_flutter\lib\widgets\status_bar.dart is the bar `WorkspaceScreen` mounts (workspace_screen.dart:53). `_SyncStatusBadge` (:127-152) has the comment `// Static "Synced" display - no async provider modifications` and unconditionally renders a green `Icons.cloud_done_outlined` + the literal string 'Synced' â€” it reads no state at all. `_CyanLensBadge` (:57-97) is a static gradient chip, also state-free. There is no connection-tier badge, no transfers button/popover, no tenant badge, and no profile button. Object/peer counts come from a 3-second `Timer.periodic` polling `CyanService.instance` (:162-198) rather than the seam. `ParityStatusBar` (lib/widgets/parity/parity_status_bar.dart:28) DOES implement `_ObjectCountBadge`, `_PeerCountBadge`, `_ConnectionTierBadge` and `_MeshStateBadge` off `mesh_status_provider.dart` â€” and `mesh_status_provider` is referenced only by shell_provider.dart, parity_status_bar.dart and test/status_sync_test.dart.
- **Gap:** The Windows status bar tells the operator they are Synced whether or not they are, and never tells them their connection tier, in-flight transfers, tenant, or lens connectivity. A partition or a stalled sync is invisible â€” the one place the Mac surfaces it is a constant.
- **Fix sketch:** Delete lib/widgets/status_bar.dart and mount `ParityStatusBar` (already wired to meshStatusProvider) in the shell gutter, then add the Mac's remaining segments to it: tenant chip, lens connection dot, and the active-transfers button + popover.

### [major] Ten Mac views have no Dart counterpart file of any kind
*id: `mac-views-with-no-dart-file`*

- **Mac does:** Present in cyan-iOS and reachable there: Views/AEQueueView.swift (the assistant-editor landing, routed to by WorkspaceViewNew.swift:335-341 via `RoleLanding.surface == "ae_queue"`), Views/ForgeView.swift (custom-tool authoring, the target of MarketplaceView's Build-a-custom-tool banner), Views/CloudOperationsView.swift (the 4-lane live console opened from the rail â€” IconRail.swift:121-130 -> WorkspaceViewNew.swift:144-146), Views/NotesStructuringView.swift, Views/Components/DiagramOverlayView.swift + DiagramResultPanel.swift, Views/Components/MentionPicker.swift, Views/Components/MonacoEditorView.swift, Views/Components/CanvasAIChatBar.swift, Views/Components/PluginBadge.swift, Views/Components/SkillInfo.swift.
- **Flutter does:** Case-insensitive `grep -rli` across ALL of C:\cyan\cyan_flutter\lib\ returns ZERO files for: AEQueue, NotesStructuring, CloudOperations, DiagramOverlay, MentionPicker, MonacoEditor, CanvasAIChatBar, PluginBadge, SkillInfo. `Forge` matches only `forgeEntryGate` in lib/providers/marketplace_provider.dart:55-66 and card-shape comments in lib/lens/ â€” there is no forge intake surface, and parity_marketplace.dart:56-57 declares `onBuildCustomTool` as an unimplemented host hook. PARITY_TRACKER.md rows 23-32 (constitution editor, board files, chat lane, review loop/run audit, AE queue, Forge, roster/transfer/profile/settings/auth, cloud ops, region composer, status bar) are the only rows with a BLANK status column â€” the tracker itself never claimed these.
- **Gap:** The assistant-editor persona has no landing surface on Windows (the Mac routes that role straight to AEQueueView at sign-in). The marketplace's headline call-to-action opens nothing even when unlocked. The Mac's dedicated Cloud Operations console has no host (the parity ops faces exist but `OpsScaffold` is itself unreachable â€” nothing in lib/ references it outside parity_ops_cost/efficiency/runs). Notes structuring, @-mentions, the Monaco code editor and diagram overlays simply do not exist.
- **Fix sketch:** These are new-file work, not wiring. Prioritise the two that block already-built surfaces: give `OpsScaffold` a rail door (unblocks the three green ops faces at once) and give `onBuildCustomTool` a forge intake so the marketplace's primary CTA is not inert. AEQueueView, NotesStructuringView and the component-level views (MentionPicker, MonacoEditorView, DiagramOverlay) are separate ports.

## AXIS — "With notes" vs "without notes" â€” can the operator see and drive both, and understand which one is happening?

**State:** On Windows today the operator can drive NEITHER lane from the app. The whole "with notes" half â€” lens structuring (`/notes/structure`), note-as-brief drafting (`/generate`) and ledger transpile (`/transpile`) â€” has no widget, no provider and no method on the `LensApi` seam; `lib/lens/lens_api.dart` carries runs/marketplace/nudges/asks/decisions only, and `integration_test/dcc_matrix_w2_test.dart:53-56` proves the lane is live on this box by hand-rolling its own `HttpClient` "deliberately NOT through `LensApi`". The "without notes" lever is worse than missing: `ParityConstitutionEditor` is a complete 612-line port that no production widget mounts â€” its only callers are `test/constitution_editor_test.dart`, so the "change the constitution â†’ behaviour changes" demo cannot be performed in the running app. Because the two write doors that carry `kind` (`notePutScoped`, `notePutAnchored`) have zero production callers, the ledger renders DECISIONS and HOUSE RULES headers that nothing on Windows can ever fill, and no surface anywhere signals whether a step came from a note or from the house rules.

### [blocker] The notes structuring lane (freeform â†’ typed proposals â†’ confirm) does not exist in Flutter at all
*id: `structuring-lane-not-drawn`*

- **Mac does:** Cyan/Cyan/Views/NotesStructuringView.swift â€” `NotesStructuringViewModel.structure()` (L143-196) POSTs `/api/v1/notes/structure` (or `/notes/structure/report` for the shot-log lane, L152), fills `proposals`/`rejected`; `NotesStructuringView` (L227-428) draws the composer with the Note|Shot-log lane picker (L261-267, id `notes.lane.picker`), the "auto-accept OFF Â· you confirm every note" caption (L269), a card per proposal with kind/scope/confidence, the VERBATIM `source_span` receipt (L375-381) and per-proposal Discard/Confirm (L389-394); `confirm(_:)` (L200-217) writes a real typed note via `sendCommand` `{"type":"PutNote", kind, scope, payload, author_role}`; the "Not structured" rejected section is L410-427. It is reachable from `NotesEditorView.swift:404-411` (segmented picker Editor|Structure|Constitution) mounting it at L310.
- **Flutter does:** ABSENT. `grep -rn "structure\|transpile" lib/` returns only unrelated hits (`component_bridge.dart:491` comment, `spatial_note.dart` `IntentStruct`, `spine_controller.dart` `transpiledOps`, and the disclaimer comment in `parity_notes_ledger.dart:132`). `lib/lens/lens_api.dart` `abstract class LensApi` (L120-180) declares runs/run/retry/approve/reject/browseMarketplace/nudges/asks/decisions/health/answerAsk/dismissAsk/reactToDecision/resolveBlocker â€” no structuring method; `LensApiHttp` (L525-776) hits no such path. No `NoteProposal`/`RejectedSpan`/`sourceSpan` type exists in lib/. `integration_test/dcc_matrix_w2_test.dart:53-56` says it outright: "One POST to the live lens. Deliberately NOT through `LensApi`: that seam has no structuring method yet (row 15's blocked half)" â€” and then opens its own `HttpClient` to `/api/v1/notes/structure` (L56-74).
- **Gap:** On the Mac the operator types "punchy, warm grade, keep it legal, -14 LUFS", presses Structure, and gets typed proposals each quoting the exact words they wrote, with Confirm/Discard per proposal and an honest list of what the lane refused. On Windows there is no Structure tab, no button, no lane â€” the freeform-note-to-typed-note step of the WITH-NOTES path is simply not on screen.
- **Fix sketch:** Add `Future<NoteStructuring> structureNotes({required String boardId, required String text, bool reportLane = false, String? authorRole})` to `LensApi` + `FakeLensApi` (mirroring `LensHTTPStepDrafter`'s envelope handling), a `NotesStructuringController`, and a `ParityNotesStructuring` widget; mount it behind a third tab on the Notes face (see the constitution finding â€” the same picker carries both).

### [blocker] "Create workflow" from the notes ledger (the /transpile lane) is deliberately not drawn
*id: `create-workflow-from-notes-unreachable`*

- **Mac does:** Cyan/Cyan/Views/BoardNotesLedgerView.swift `actionsRow` (L142-185): the "Create workflow" button (L143-156, id `notes.createFromNotes`) calls `intent.authorWorkflowFromNotes(viewModel.notes)`. Cyan/Cyan/ViewModels/NotesIntentViewModel.swift `authorWorkflowFromNotes(_:)` (L389-422) maps the ledger to `LensWireNote`s, reads `backend.constitutionEffective(boardId:)` (L404-406), POSTs `/api/v1/transpile` via `LensHTTPTranspiler` (L218-260), then `landSteps` (L426-457) lands each bound step through the exact composer path (`WorkflowViewModel.addStep`), calls `pipelineCompile`, and posts `.switchToBoardFace` to the notebook face for human review. It is live in the shipping app: `BoardContainerViewNew.swift:425-427` passes a non-nil `intentViewModel` into `NotesEditorView`, which passes it to `BoardNotesLedgerView` at `NotesEditorView.swift:338`.
- **Flutter does:** ABSENT BY DECISION. `lib/widgets/parity/parity_notes_ledger.dart:130-134`: "'Create workflow' â€” the reference's other action â€” is a LENS TRANSPILE over HTTP (NotesIntentViewModel â†’ the /transpile lane), so it belongs to the LensApi seam and is deliberately NOT drawn here". The ledger's action row (L135-149) contains only the `notes-ledger-copy` pill. No `transpile` method on `LensApi` (`lib/lens/lens_api.dart:120-180`); no `NotesIntentController` in `lib/providers/`; `grep -rn "authorWorkflow" lib/` â†’ nothing.
- **Gap:** On the Mac, a board whose ledger holds a colour note and a house rule is one click from a reviewed workflow, with note/rule provenance and an IR-cached/fresh cost line. On Windows the notes sit in the ledger and there is no action that turns them into steps â€” the WITH-NOTES path dead-ends at the ledger.
- **Fix sketch:** Add `Future<LensTranspiled> transpileNotes({required String boardId, required List<LensWireNote> notes, EffectiveConstitution? constitution})` to `LensApi`/`FakeLensApi` (port `LensHTTPTranspiler.extract` incl. `composeStepLine` â€” the `via <tool>` and `/needs-approval` suffixes are load-bearing â€” and `provenanceLines`), a `NotesIntentController` that seats `backend.constitutionEffective(boardId)` on the wire and lands steps via the existing workflow authoring provider + `pipelineCompile`, then draw the pill in `parity_notes_ledger.dart` next to `notes-ledger-copy`.

### [blocker] The Notes toolbar's "Author workflow with Lens" leg (note-as-brief â†’ /generate) and its outcome banner are missing
*id: `author-workflow-with-lens-absent`*

- **Mac does:** Cyan/Cyan/Views/NotesEditorView.swift mounts `NotesAuthorWithLensButton` in the toolbar (L439-441); the button (L923-955, id `notes.authorWithLens`) saves the note then calls `intent.authorWorkflow(from: notesViewModel.content)`. `NotesIntentViewModel.authorWorkflow(from:)` (L348-380) reads `constitutionEffective`, POSTs `/api/v1/generate` through `LensHTTPStepDrafter` (L108-157) with the `constitution` seat (L126-139), and lands the steps. `NotesIntentBanner` (L960-1019, mounted at L316-318) is the outcome strip: `notes.intent.error` on failure, `notes.intent.success` ("N steps drafted â€” review them on the Workflow face, then press Run yourself.") and `notes.intent.savings` (the cache/cost money line from `costLine`, L463-481).
- **Flutter does:** ABSENT. `lib/widgets/parity/parity_notes_view.dart` `_toolbar()` (L180-250) contains only: doc icon, `notes-filename`, `notes-filetype` chip, `notes-save-problem`, `notes-save-state` dot and the `notes-save` icon. There is no intent banner in `build()` (L124-176) â€” the only banner is `notes-error` (L139-146), which is the editor's own save error. `grep -rn "api/v1/generate\|drafted\|costLine\|LensStepDrafter" lib/` â†’ no matches; `lib/providers/` has no notes-intent controller.
- **Gap:** On the Mac the note the operator just typed IS the brief: one toolbar press drafts steps from it, and the banner then tells them how many landed and what the draft cost (or that the cache saved it). On Windows the Notes face can only save a document; nothing turns the note into a workflow and nothing reports a lens outcome.
- **Fix sketch:** Add `Future<LensDraft> generateSteps({required String boardId, required String brief, EffectiveConstitution? constitution})` to `LensApi`/`FakeLensApi`, port `NotesIntentViewModel.costLine` + `parseStepLines` verbatim into the controller, add the toolbar pill to `_toolbar()` and an intent banner above the editor body in `parity_notes_view.dart`.

### [blocker] ParityConstitutionEditor is fully built and mounted by nothing â€” the house-rules lever is unreachable from the running app
*id: `constitution-editor-unmounted`*

- **Mac does:** Cyan/Cyan/Views/NotesEditorView.swift declares `enum NotesFaceMode { case editor, structure, constitution }` (L277), draws the segmented picker "Editor | Structure | Constitution" (L404-411, id `notes.mode.picker`) and mounts `ConstitutionEditorView(boardId:)` at L311-313. `ConstitutionEditorView.swift` (L142-279) shows the effective constitution + hash (`constitution.hash`, L170-174), the Add-a-rule editor (L201-229, `constitution.rule.field` / `constitution.rule.add`) writing `PutNote` with scope+kind (`addRule`, L122-139), and the authorship list.
- **Flutter does:** UNREACHABLE. `lib/widgets/parity/parity_constitution_editor.dart` is a complete 612-line port (read via `constitutionPreviewProvider`/`constitutionHardRulesProvider`/`boardAuthorshipProvider`, write via `notePutScoped` at L87-92). But `grep -rn "ParityConstitutionEditor" lib/ test/ integration_test/` returns hits ONLY inside that file itself and inside `test/constitution_editor_test.dart` (L23/60/83/107). `lib/widgets/parity/parity_board_container.dart` `_face(face)` (L137-141) mounts exactly three faces â€” `ParityWorkflowView`, `ParityNotesView`, `ParityDashboardView` â€” and `parity_notes_view.dart` has no mode picker of any kind. `grep -rn "constitution" lib/` outside the editor file hits only `lib/ffi/*` and `lib/providers/settings_identity_provider.dart`. PARITY_TRACKER.md row 23 ("Constitution editor | ConstitutionEditorView | FFI |") has an empty status column.
- **Gap:** The WITHOUT-NOTES lever â€” the product owner's stated demo ("add a constitution note line, hit Retry, the endcard changes") â€” cannot be pulled from the Windows app at all. The operator cannot read the board's effective constitution, cannot see its hash or hard rules, and cannot add a house rule, because no navigation path reaches the widget that does all three. Every route into the constitution on the Mac goes through the Notes-face mode picker, and Flutter's Notes face has no picker.
- **Fix sketch:** Port `NotesFaceMode` as a three-way segmented control in `parity_notes_view.dart`'s `_toolbar()` (keys `notes-mode-editor` / `-structure` / `-constitution`) and switch the body between the current editor column, the new structuring surface, and `ParityConstitutionEditor(boardId: widget.boardId)` â€” the widget and its providers already work, only the mount point is missing.

### [major] No production code path can author a decision, constitution or preference note â€” two of the ledger's three sections are unfillable on Windows
*id: `no-typed-note-authoring`*

- **Mac does:** Two doors write typed notes on the Mac. (1) Chat promote: `Cyan/Cyan/ViewModels/ChatLaneViewModel.swift` `promote(message:target:)` (L100-130) sends `{"type":"PutNote", kind: decision|editor-note, origin_ref: "chat:<id>", anchorâ€¦}` with a deterministic id (L84-87), bound to âŒ˜â‡§E in `Views/BoardChatLane.swift:103-105`; the ledger's own empty state instructs it â€” `BoardNotesLedgerView.swift:196` "Promote a chat message (âŒ˜â‡§E) to record it here." (2) `ConstitutionEditorView.addRule` (L122-139) writes `kind: constitution|preference`. `NotesLedgerViewModel` then groups by kind into decisions / houseRules / editorNotes (L254-263).
- **Flutter does:** `lib/widgets/parity/parity_notes_ledger.dart` `_add()` (L60-67) â†’ `NotesLedgerController.addNote` (`lib/providers/notes_ledger_provider.dart:189-200`) â†’ `_backend.notePut(boardId, trimmed)`, and `CyanBackend.notePut` (`lib/ffi/cyan_backend.dart:538-539`) takes only `{noteId, tenantId}` â€” it has NO `kind` parameter, so an editor-note is the only thing it can mint. The kind-carrying doors exist but are dead: `notePutAnchored` (`cyan_backend.dart:551-563`, implemented at `cyan_backend_ffi.dart:1582-1618` with kind/anchor/originRef/authorRole) has zero callers outside `test/notes_ledger_test.dart:292,314` and `integration_test/notes_ledger_face_test.dart:132`; `notePutScoped` (`cyan_backend.dart:601-602`) has exactly one caller â€” the unmounted `parity_constitution_editor.dart:87`. `lib/providers/board_chat_controller.dart` exposes only `load()` (L94), `send()` (L124), `delete()` (L142) â€” no promote, and `grep -n "note" lib/providers/board_chat_controller.dart` returns nothing.
- **Gap:** `ParityNotesLedger` draws 'DECISIONS' and 'HOUSE RULES' section headers (L178-180) that no action in the running Windows app can ever populate â€” the operator can only ever add a plain note. The âŒ˜â‡§E chatâ†’decision promote lane that the Mac ledger's own empty state advertises does not exist, and the ledger's Dart empty state was reworded to hide it (`parity_notes_ledger.dart:346` "Anything recorded here is kept on the board" vs Swift's "Promote a chat message (âŒ˜â‡§E)"). Since transpile reads `kind`/`craft_role` off each note, this also starves the WITH-NOTES lane of the typed input it is supposed to consume.
- **Fix sketch:** Give the ledger composer a kind selector (decision | note | constitution | preference) routed through the existing `notePutAnchored`, and add a promote action + âŒ˜â‡§E binding on `parity_chat_view.dart` rows calling `notePutAnchored(..., kind: 'decision', originRef: 'chat:$messageId', noteId: 'chat-promote:decision:$messageId')` â€” the seam method already exists and is engine-proved by `integration_test/notes_ledger_face_test.dart`.

### [major] The Workflow face never shows which steps a note is driving â€” stepNoteCounts is not ported
*id: `no-step-note-provenance-badge`*

- **Mac does:** Cyan/Cyan/ViewModels/NotesLedgerViewModel.swift `stepNoteCounts` (L267-272) reduces the ledger to `anchor_id â†’ count` ("the Workflow face badges rows from this map (no per-row FFI)"). `Views/BoardContainerViewNew.swift:419` threads it into `WorkflowView(..., stepNoteCounts: notesLedgerViewModel.stepNoteCounts)`; `Views/WorkflowView.swift:30` holds it, L510 passes `noteCount: stepNoteCounts[step.anchorUid] ?? 0` into `StepRow`, and the badge renders at L962-976 with the tooltip "N ledger note(s) anchored to this step â€” see the Notes face" (id `workflow.step.notes.<uid>`).
- **Flutter does:** ABSENT. `lib/providers/notes_ledger_provider.dart` `NotesLedgerState` (L79-138) exposes `decisions`/`houseRules`/`editorNotes`/`byId` and nothing keyed by `anchorId`; `grep -rn "stepNoteCounts" lib/` â†’ no matches. `lib/widgets/parity/parity_workflow_view.dart` contains the word "note" only in two header comments (L26, L95) â€” the step row (built around L518-597) draws no note badge and the file never reads the ledger.
- **Gap:** On the Mac, a step that a reviewer's note is steering carries a yellow note badge on the Workflow face, so the operator can tell at a glance which parts of the run are note-driven versus house-driven. On Windows every step row looks identical whether it came from a note, a transpile or a template â€” the single visual signal that distinguishes the two lanes is gone.
- **Fix sketch:** Add `Map<String,int> get stepNoteCounts` to `NotesLedgerState` (filter `anchorKind == 'step'`, group by `anchorId` â€” `CyanNote.anchorId` already decodes, `lib/ffi/parity_models.dart:1938,1983`), watch `notesLedgerProvider(boardId)` from `parity_workflow_view.dart`, and draw the count chip on the step row keyed by the step's stable uid.

### [minor] Even once mounted, the ported constitution editor drops tenant scope and the decisionâ†’step promote
*id: `constitution-editor-missing-tenant-scope-and-promote`*

- **Mac does:** Cyan/Cyan/Views/ConstitutionEditorView.swift L210-213: the scope picker offers three tags â€” `board`, `group`, `tenant`. L237-277 the authorship section gives every authored note a `â†’ Step` button (id `notes.promote.step.<id>`, L268) wired to `promoteToStep(_:)` (L110-120), which lands the note's text as a real step through `WorkflowViewModel.addStep` â†’ `cyan_save_notebook_cell`, then calls `pipelineCompile` and flips the row to "Promoted to step" (L263-266).
- **Flutter does:** `lib/widgets/parity/parity_constitution_editor.dart` `enum RuleScope { board, group }` (L33-39) â€” the `tenant` case does not exist, so the segmented control at L377-383 can only ever offer two scopes and a tenant-wide house rule is structurally unauthorable. `_Authorship` (L430-498) renders kind chip / author / role / scope / text for each note and emits no button at all â€” `grep -n "promote" lib/widgets/parity/parity_constitution_editor.dart` â†’ no matches.
- **Gap:** A studio-exec authoring a rule for the whole tenant cannot express it (only board or group), and the other direction of the notesâ†”workflow loop â€” turn this recorded decision into a runnable step â€” has no affordance, so the operator can read authorship but cannot act on it.
- **Fix sketch:** Add `tenant('tenant')` to `RuleScope` (resolving the tenant anchor the way `boardGroupIdProvider` resolves the group), and add a `â†’ Step` pill per authorship row calling the existing workflow authoring provider's addStep + `pipelineCompile`, with a local promoted-set for the "Promoted to step" confirmation.

## AXIS — Template clone â†’ marketplace plugin auto-install (does a cloned template get its plugins installed, the way the Mac app does?)

**State:** On Windows today this axis is not merely divergent â€” it is unreachable. The parity faces that would carry it (ParityTemplatePicker, ParityWorkflowView, ParityBoardContainer, ParityMarketplace, ParityHomeShell) are complete, well-written and mounted ONLY in `test/`; the app `main.dart` actually runs is the legacy `WorkspaceScreen`, whose four doors (explorer/allBoards/chat/events) contain no Market, and whose board faces (Canvas/Notebook/Notes) contain no Workflow face and therefore no Templates button â€” `grep -rn "emplate" lib/widgets/board_detail_view.dart lib/widgets/icon_rail.dart lib/screens/` returns nothing at all. Underneath that navigation gap the port is also missing three real behaviours the Mac ships: the client-side clone-time install of every plugin a template's STEPS @mention, the cyan-media default-plugin provisioning that every group gets for free on macOS, and the WorkflowDraftStore that makes "Use in a workflow" a real action. The engine is not the problem â€” `cyan_backend.dll` exports `cyan_workflow_from_template`, `cyan_template_clone_outcome` and `cyan_install_plugin_bundle`, and carries the `/api/v1/marketplace/bundle/` + `plugin_installs`/`already_present`/`failed` strings, so the land-leg and the engine's own G1 auto-install are both present and waiting.

### [blocker] The Marketplace is unreachable from the running Windows app â€” there is no Market door at all
*id: `market-door-absent`*

- **Mac does:** Cyan/Cyan/Views/WorkspaceViewNew.swift:322 â€” `MarketplaceView(targetGroupId: selectedGroup?.id ?? fileTreeViewModel.selectedGroupId)` is one of the rail's surfaces; MarketplaceView.swift:44-76 binds targetGroupId, role and installerXaeroId on appear.
- **Flutter does:** lib/main.dart:142 returns `const WorkspaceScreen()`. lib/providers/navigation_provider.dart:7-12 declares `enum ViewMode { allBoards, explorer, chat, events }` â€” no market member; lib/screens/workspace_screen.dart:69-80 switches over exactly those four. `grep -rni "marketplace|market" lib/widgets/icon_rail.dart lib/screens/workspace_screen.dart lib/providers/navigation_provider.dart` returns ZERO hits. `ParityMarketplace` is constructed at exactly one place in lib/ â€” parity_home_shell.dart:66 â€” and `ParityHomeShell` itself is referenced nowhere in lib/ (grep -rn ParityHomeShell lib/ hits only its own declaration at parity_home_shell.dart:31-32 and parity_icon_rail.dart:7's comment); the only live references are test/shell_test.dart.
- **Gap:** A Windows operator cannot open the storefront, cannot browse plugins, and cannot install one â€” the entire manual fallback for a template whose plugin did not auto-install does not exist in the shipping navigation. On the Mac it is a rail click away.
- **Fix sketch:** Add `market` to `ViewMode` (navigation_provider.dart:7), a fifth rail button in lib/widgets/icon_rail.dart, and a `case ViewMode.market: content = ParityMarketplace(groupId: ref.watch(selectionProvider).groupId, sessionRole: <session role>, onUse: <queue draft step>);` arm in workspace_screen.dart:69.

### [blocker] The template picker â€” and therefore every clone and every clone-time auto-install â€” is unreachable from the running app
*id: `template-picker-unreachable`*

- **Mac does:** Cyan/Cyan/Views/WorkflowView.swift:349-357 â€” a toolbar button with accessibilityIdentifier `workflow.templates` sets `showTemplates = true` and `.sheet` presents `TemplatePickerSheet(...)`; the Workflow face is a board face, and BoardContainerViewNew.swift:73 builds `TemplatesViewModel(boardId: boardId, groupId: groupId)` for it.
- **Flutter does:** `ParityTemplatePicker` is constructed at exactly one place in lib/: parity_workflow_view.dart:246 (inside `_openTemplates`). `ParityWorkflowView` is constructed at exactly one place: parity_board_container.dart:138 (`BoardFace.workflow => ParityWorkflowView(...)`). `ParityBoardContainer` is constructed NOWHERE in lib/ â€” `grep -rn "ParityBoardContainer(" lib/` returns only its own constructor declaration at parity_board_container.dart:55. What the running app actually shows is lib/widgets/board_detail_view.dart:22-30, a *different* `enum BoardFace { canvas, notebook, notes }` dispatched at :178-185 to `_CanvasView` / `_NotebookView` / `_NotesView`; `grep -n "emplate" lib/widgets/board_detail_view.dart` returns nothing.
- **Gap:** There is no button, menu item or shortcut anywhere in the running Windows app that opens a workflow template. The operator cannot clone a template, so the auto-install question never even arises for them; on the Mac it is one button on the Workflow face.
- **Fix sketch:** Mount `ParityBoardContainer` (which already routes `BoardFace.workflow â†’ ParityWorkflowView â†’ ParityTemplatePicker`) in place of `BoardDetailView` at workspace_screen.dart:110-113, or add the Workflow face + a Templates button to board_detail_view.dart that shows `ParityTemplatePicker(boardId: â€¦)` in a Dialog.

### [blocker] cyan-media, the default plugin every macOS group gets for free, is never provisioned on Windows
*id: `default-plugins-never-provisioned`*

- **Mac does:** Cyan/Cyan/Models/DefaultPlugins.swift:16-56 â€” `DefaultPlugins.ids = ["cyan-media"]`, `ensure(groupId:backend:)` reads the app-shipped bytes (`Bundle.main.url(forResource:withExtension:"cyanplugin")`, the file Cyan/Resources/cyan-media.cyanplugin) and lands them via `backend.installPluginBundle(...)`. Called from ViewModels/FileTreeViewModel.swift:229 (on `.treeLoaded`, for EVERY group in the tree) and :279 (on `.groupCreated`). TemplatesViewModel.swift:291 then does `.subtracting(DefaultPlugins.ids)` precisely because this path already owns cyan-media.
- **Flutter does:** ABSENT. `grep -rn "cyan-media|DefaultPlugins|defaultPlugins" lib/` hits only seed strings inside lib/ffi/fake_cyan_backend.dart (:3757 'via @cyan-media.conform', :3814) and a comment in lib/ffi/parity_models.dart:3134 â€” no provisioning code. `find C:/cyan/cyan_flutter -name "*.cyanplugin" -not -path "./build/*"` returns NOTHING, and pubspec.yaml's `flutter:` block is `uses-material-design: true` with no `assets:` key, so no bundle could be shipped even if code wanted it. lib/providers/file_tree_provider.dart has no ensure hook (its only group writes are `createGroup` at :249 and :743).
- **Gap:** A brand-new group on Windows has an empty Plugins workspace. Every `@cyan-media.*` step a cloned template lands (the conform/probe steps of the media seeds) binds to a plugin that is not there and can only pend â€” and because cyan-media ships as an app resource rather than a lens listing, no marketplace visit would fix it either. The Mac guarantees it with zero clicks, on group creation and on every tree load.
- **Fix sketch:** Ship `assets/plugins/cyan-media.cyanplugin` (declare it under `flutter: assets:` in pubspec.yaml) and add a `DefaultPlugins.ensure(groupId, backend)` port that loads it with `rootBundle.load` + `base64Encode` and calls `CyanBackend.installPluginBundle`, invoked from the file-tree provider wherever a group appears â€” the mirror of FileTreeViewModel.swift:229/:279.

### [major] The Dart clone path does zero client-side plugin resolution â€” it never fetches or installs the plugins a template's steps @mention
*id: `clone-no-step-bound-autoinstall`*

- **Mac does:** Cyan/Cyan/ViewModels/TemplatesViewModel.swift:286-306 `autoInstallTemplatePlugins(for:)` â€” required set is `Set(template.steps.compactMap(\.plugin)).subtracting(DefaultPlugins.ids)`, and for each id it does `try? await fetcherBox.fetchBundle(pluginId:)` then `backendBox.installPluginBundle(groupId: group, pluginId: pluginId, bundleB64: b64)`. It is called at :207 from inside `clone()`, BEFORE `workflowFromTemplate` â€” comment: "GAP 1 â€” auto-install the template's plugins from Lens BEFORE the clone, so the cloned board is immediately runnable (WALK 1) with zero manual installs". The VM takes a `PluginBundleFetching` in init (:127-135). scripts/walk1_template_autoinstall.sh is the acceptance test for exactly this (clone `builtin:frameio-review-loop` into a fresh group, assert frameio lands, "no Marketplace visit").
- **Flutter does:** ABSENT. lib/providers/templates_provider.dart:154-159 â€” `TemplatesController` takes only a `CyanBackend`, no fetcher. `clone()` at :212-272 goes from the replace-clear straight to `await _backend.workflowFromTemplate(templateId, boardId, tenantId: state.tenantId)` at :245. The file's imports (:25-30) do not include `../services/plugin_bundle_fetcher.dart`. `grep -rn "installPluginBundle" lib/` shows the only call site in the whole app is lib/providers/marketplace_provider.dart:347, and `grep -rn "plugin_bundle_fetcher|fetchBundleB64" lib/` shows the fetcher is wired only into marketplace_provider.dart.
- **Gap:** Windows depends entirely on the ENGINE's declared auto-install registry (`CyanTemplate.autoInstallSet`, lib/ffi/parity_models.dart:2821 = plugins flagged `auto_install`). The Mac installs from the STEP BINDINGS â€” a strictly different, usually larger set. A template whose step says `@frameio â€¦` but whose registry row is not flagged `auto_install` (the fake's own seed data models exactly this case at fake_cyan_backend.dart:3810-3812, 'Declared but NOT auto-installed') is installed on macOS and silently left pending on Windows.
- **Fix sketch:** Give `TemplatesController` a `PluginBundleFetcher` (reuse `pluginBundleFetcherProvider`, marketplace_provider.dart:30) and add a best-effort `_autoInstallTemplatePlugins(templateId)` awaited immediately before the `workflowFromTemplate` call at templates_provider.dart:245: required = step plugins minus the default ids, then `fetchBundleB64` â†’ `_backend.installPluginBundle(state.tenantId, id, b64)`, swallowing failures so an offline clone still lands.

### [major] Even inside the Marketplace face, Install can never run: no group id is ever threaded in
*id: `marketplace-install-no-group`*

- **Mac does:** Cyan/Cyan/Views/MarketplaceView.swift:68-69 â€” `.task(id: targetGroupId) { viewModel.targetGroupId = targetGroupId }`, fed from WorkspaceViewNew.swift:322; MarketplaceViewModel.swift:441 then resolves `groupId ?? targetGroupId ?? Self.launcherGroupOverride` (the `cyan_group_id` default, :420-424) before installing.
- **Flutter does:** lib/widgets/parity/parity_home_shell.dart:66 â€” `ShellDoor.market => const ParityMarketplace()`, passing no `groupId` (nor `sessionRole`, `onUse`, `onBuildCustomTool`), even though `selectedGroupIdProvider` exists at lib/providers/selection_provider.dart:172. That null flows to `MarketplaceController._groupId` (marketplace_provider.dart:258, 268), and `install()` at :324-331 short-circuits before the fetcher or the seam: `final group = _groupId ?? ''; if (group.isEmpty) { _finish(card, ok: false, message: 'Select a group first â€” â€¦'); return; }`. There is no launcher fallback â€” `CYAN_GROUP_ID` is read only for the lens config (lib/lens/lens_api.dart:79) and never consulted by the marketplace controller.
- **Gap:** Every Install button in the storefront (shelf pill at parity_marketplace.dart:591-602, Get page at parity_marketplace_detail.dart:490-491) terminates in the refusal banner "Select a group first" no matter what the operator has selected â€” the app offers no way to satisfy it. Fixing the missing Market door alone would leave this dead.
- **Fix sketch:** Pass `groupId: ref.watch(selectedGroupIdProvider)` (and `sessionRole`) wherever `ParityMarketplace` is mounted, and add the `MarketplaceViewModel.launcherGroupOverride` equivalent as a fallback in `MarketplaceController` â€” `_groupId ?? Platform.environment['CYAN_GROUP_ID']`.

### [major] "Use in a workflow" â€” the storefront's primary CTA on every tile and Get page â€” does literally nothing
*id: `use-in-workflow-dead`*

- **Mac does:** Cyan/Cyan/Views/MarketplaceView.swift:722-749 `useInWorkflowFooter` calls `WorkflowDraftStore.shared.queue(pluginId: card.id, pluginName: card.name, stepText: card.workflowStepText)` UNCONDITIONALLY (the `onUseInWorkflow?(card)` callback at :728 is extra, not the mechanism); MarketplaceDetailView.swift:99-115 `useInWorkflowRow` does the same at :102. WorkflowView.swift:107 drains it â€” `let drafts = WorkflowDraftStore.shared.drain(); for d in drafts { _ = viewModel.addStep(text: d.stepText) }` â€” so the queued step really appears as a step on the next board's Workflow.
- **Flutter does:** lib/widgets/parity/parity_marketplace.dart:530 â€” the footer button's entire body is `onTap: () => onUse?.call(card)`; lib/widgets/parity/parity_marketplace_detail.dart:391 â€” `widget.onUse?.call(card)`. `onUse` is null at the only mount (parity_home_shell.dart:66 `const ParityMarketplace()`). There is no draft store to fall back on: `grep -rni "draftStore|workflowDraft|queueStep|pendingDraft|workflowStepText" lib/` returns ZERO hits.
- **Gap:** The operator presses the big cyan primary button on a plugin card and nothing happens â€” no queued step, no confirmation, no state change. On the Mac the same press queues a real plain-English step that the next Workflow face adopts, with an honest "Queued for a board's Workflow / Queuing isn't installing" confirmation.
- **Fix sketch:** Port `Models/WorkflowDraftStore.swift` as a small Riverpod provider (`workflowDraftQueueProvider`) written by both marketplace surfaces unconditionally and drained in `ParityWorkflowView`'s init via `addStep`, the way WorkflowView.swift:107 does; keep `onUse` as the optional extra hook it is on the Swift side.

### [major] The Dart clone blocks the picker for up to 90 seconds inline, then reports "nothing landed" â€” a claim it cannot make
*id: `clone-poll-blocks-and-lies`*

- **Mac does:** Cyan/Cyan/ViewModels/TemplatesViewModel.swift:208-222 â€” `clone()` awaits only `workflowFromTemplate` and then fires the poll detached: `Task { [weak self] in await self?.pollCloneOutcome(templateName: name) }`, returning `ok` immediately. TemplatePickerSheet.swift:161-175 closes the sheet and calls `onCloned()` right away; the outcome arrives later as `TemplateCloneToast` on the Workflow face (WorkflowView.swift:1125-1157). :226-227 states the contract: "Old engine (verb absent) â‡’ every poll returns nil â‡’ no toast, exactly the pre-verb behavior" â€” an absent outcome is silent, never an error.
- **Flutter does:** lib/providers/templates_provider.dart:257 â€” `final outcome = await _pollOutcome(attempts: attempts, interval: interval);` is awaited INLINE inside `clone()`, with defaults `attempts = 90, interval = const Duration(seconds: 1)` (:216-217). While it runs, `state.cloningId` stays set, so parity_template_picker.dart:445 renders "Cloningâ€¦" and :431 disables the button. On timeout, :259-265 sets `error: 'The clone did not report back â€” nothing landed on this board.'` and returns false, so `widget.onCloned?.call()` at parity_template_picker.dart:127 never fires.
- **Gap:** The operator watches a frozen "Cloningâ€¦" for up to a minute and a half and is then told nothing landed â€” but `cyan_workflow_from_template` is fire-and-forget (documented at lib/ffi/ffi_helpers.dart:1906-1908), so the steps may well be on the board. The Mac dismisses the sheet the instant the engine takes the dispatch and reports the outcome later, or not at all.
- **Fix sketch:** Split templates_provider.dart:212-272: return `true` as soon as `workflowFromTemplate` resolves, and run `_pollOutcome` in an unawaited future that only publishes `outcome`/`clonedName` when non-null â€” dropping the timeout error entirely, matching TemplatesViewModel.swift:217 and :226-227.

### [minor] "Build a custom tool" (the forge) is permanently locked and inert on Windows
*id: `forge-entry-permadead`*

- **Mac does:** Cyan/Cyan/Views/MarketplaceView.swift:73 `viewModel.setRole(identityVM.role)` and :99-100 re-set it when the session's role changes; MarketplaceViewModel.swift:298-304 derives `canBuildCustomTool = authorize(.codegenPlugin, for: role)` so an Admin/Owner really opens the forge intake.
- **Flutter does:** lib/widgets/parity/parity_marketplace.dart:88-91 builds `_BuildCustomToolEntry(gate: forgeEntryGate(sessionRole), onTap: onBuildCustomTool)`. At the only mount (parity_home_shell.dart:66) both `sessionRole` and `onBuildCustomTool` are null, so `forgeEntryGate(null)` (marketplace_provider.dart:66-78) always returns `locked: true, reason: 'Sign in with an admin session to build a custom tool.'`, and `_BuildCustomToolEntry`'s `onTap: gate.locked ? null : onTap` (:214) is null on both branches.
- **Gap:** An admin on Windows sees the forge entry greyed out and told to sign in as an admin â€” which they are â€” and tapping it does nothing. The other route to obtaining a plugin a template needs is therefore also closed. On the Mac the entry unlocks for Admin+ and opens the intake.
- **Fix sketch:** Thread the resolved membership role and a forge-intake callback into `ParityMarketplace` at its mount site, the mirror of MarketplaceView.swift:73/:99.


